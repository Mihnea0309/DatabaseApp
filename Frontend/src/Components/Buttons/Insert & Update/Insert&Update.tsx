import React, {useState} from 'react';
import { Link, useNavigate } from 'react-router-dom';
import './Insert&Update.css';


const InsertUpdate: React.FC = () => {

    return (
        <div>
            <button className="insert-button">Insert</button>
        </div>
    );
};

export default InsertUpdate;
